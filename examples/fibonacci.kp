// Krypto Programming Language - Fibonacci Example
// Calculate the nth Fibonacci number

int fun fibonacci(n: int) {
    if (n <= 1) {
        return n
    }
    return fibonacci(n - 1) + fibonacci(n - 2)
}

fun main() {
    // Print first 10 Fibonacci numbers
    let i = 0
    while (i < 10) {
        let fib = fibonacci(i)
        print(fib)
        i = i + 1
    }
}
